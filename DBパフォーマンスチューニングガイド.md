# DBパフォーマンスチューニングガイド

Udemy講座「SQLserver パフォーマンスチューニング　インデックス編」および『実践SQL入門 改訂版』の内容を基に、SQL Serverにおけるデータベースパフォーマンスチューニングの理論と実践テクニックを自分なりにまとめたものです。

## 1. パフォーマンスチューニングとは

データベースにおける「パフォーマンス」とは、主に**レスポンスタイム**（SQLを投げてから結果が返ってくるまでの時間）を指す。
チューニングとは、データベース設定やSQL文、インデックスを調整することで、同じ結果をより短い時間で得られるようにするプロセス。

### パフォーマンスを悪化させる主な要因

- **物理リソース不足**: CPU、メモリ、ストレージ（HDD/SSD）、ネットワーク帯域の不足。
- **非効率な実行プラン**: クエリオプティマイザが最適なデータアクセス方法を選択できていない。
- **不適切なSQL**: 非効率な書き方や結合により、無駄なデータアクセスやメモリ消費が発生している。
- **ロックの競合**: トランザクション処理中のデータ整合性を保つためのロックにより、待ち時間が発生している。
- **インデックスの欠如または不適切な設計**: データの検索に時間がかかっている。

---

## 2. SQL Server内部でのクエリ処理フロー

SQL Serverがクエリを受け取ってからデータを返すまでの流れを理解することは、チューニングの第一歩となる。

1.  **パーサー (Parser)**: SQL文の構文解析（文法チェック）。
2.  **プランキャッシュの確認**: 同じクエリの実行プランがメモリ（プランキャッシュ）にあるか確認。あれば再利用（高速）。
3.  **オプティマイザ (Optimizer)**:
    - キャッシュがない場合、データへの最適なアクセス方法（実行プラン）を作成する。
    - **統計情報**（データの分布、密度、ヒストグラムなど）を基に、複数のプランのコスト（CPU、I/O）を計算し、最もコストが低いものを選択する。
4.  **エグゼキュータ (Executor)**: 決定された実行プランに従ってデータアクセス・処理を実行。

### 統計情報の重要性

オプティマイザは「統計情報」を頼りにプランを決める。統計情報が古いと、実際のデータ分布とズレが生じ、非効率なプランが選択される原因になる。

---

## 3. インデックスの仕組み

インデックスは本の「索引」のようなもの。索引がないと本を最初から最後まで読む（全件走査）必要があるが、索引があれば該当ページへ直接アクセスできる。

### B-Tree（B木）構造

SQL Serverの標準的なインデックスは**B-Tree構造**をしている。

- **安定した性能**: キー値の間で検索速度にばらつきが少なく、計算量が $O(\log N)$ で安定している。
- **範囲検索に強い**: 等号（`=`）だけでなく、`>`、`<`、`BETWEEN` などの範囲検索も高速。

### インデックスの種類

| 特徴                  | クラスター化インデックス (Clustered Index) | 非クラスター化インデックス (Non-Clustered Index)                     |
| :-------------------- | :----------------------------------------- | :------------------------------------------------------------------- |
| **格納内容 (リーフ)** | **実データそのもの**が含まれる             | **クラスター化インデックスのキー**（実データへの参照キー）が含まれる |
| **個数制限**          | テーブルに**1つのみ**                      | テーブルに複数作成可能（最大999）                                    |
| **物理的な並び**      | データがキー順に物理的に並べ替えられる     | インデックス自体はキー順だが、実データは別の場所にある               |
| **使い分け**          | 主キー（PK）などに使用                     | 頻繁に検索条件や結合キーになる列に使用                               |

### カーディナリティ（選択率）とインデックスの有効性

インデックス設計において最も重要な概念の一つが「カーディナリティ（値のばらつき具合）」である。

- **カーディナリティが高い**: 値の重複が少なく、特定の一行を絞り込みやすい（例：ID、メールアドレス）。インデックスの効果が高い。
- **カーディナリティが低い**: 値の重複が多い（例：性別、削除フラグ）。インデックスの効果が薄い。

**注意点**:
全体の **20%〜30%以上** の行がヒットするような検索条件では、インデックスを使うよりもテーブルスキャン（全件走査）の方が速いとオプティマイザに判断され、インデックスが無視されることがある。

---

## 4. 実行プランと結合アルゴリズム

実行プランを確認する際は、データへのアクセス方法（Scan/Seek）だけでなく、テーブル同士の結合方法（Join Algorithm）にも注目する必要がある。

### データアクセス操作（Scan vs Seek）

1.  **Table Scan / Clustered Index Scan**: テーブル全件読み込み。効率が悪い。
2.  **Clustered Index Seek**: 主キー等による理想的な高速アクセス。
3.  **Index Seek**: 非クラスター化インデックスによる高速検索。
4.  **Key Lookup**: 非クラスター化インデックスから実データを取りに行く処理。コストが高い。

### 結合アルゴリズムの比較

SQL Serverが選択する主な結合アルゴリズムは以下の3つ。それぞれの特性を理解し、意図せぬ結合が選ばれていないか確認する。

#### 1. Nested Loops (入れ子ループ結合)

- **動作**: 駆動表（外側）を1行ずつ読み込み、内部表（内側）を検索して結合する。プログラムの二重ループと同じ。
- **得意なケース**: 駆動表が小さく、内部表の結合キーにインデックスがある場合。最も高速で理想的。
- **弱点**: 駆動表が大きい場合や、内部表にインデックスがない場合は極端に遅くなる。

#### 2. Hash Join (ハッシュ結合)

- **動作**: 結合キーのハッシュ値を作成し、ハッシュテーブルをメモリ上に作って結合する。
- **得意なケース**: 両方のテーブルが大きく、インデックスがない場合や、大量データを一括処理する場合。
- **弱点**: メモリ（ワーキングメモリ）を消費する。メモリ不足になると「TEMP落ち（ディスクへの退避）」が発生し、急激に遅くなる。

#### 3. Sort Merge Join (ソートマージ結合)

- **動作**: 両方のテーブルを結合キーでソートしてから、突き合わせる。
- **得意なケース**: データが既にソートされている場合や、不等号などの範囲結合を行う場合。
- **弱点**: ソート処理にコストがかかる。ハッシュ同様、メモリ不足時のTEMP落ちリスクがある。

---

## 5. 効果的なインデックス設計とチューニング

パフォーマンス向上の鍵は、**I/O（ディスク読み込み）を減らすこと**と**Key Lookup（キー参照）を回避すること**にある。

### カバリングインデックスとインデックスオンリースキャン

クエリで必要な**全ての列**をインデックスに含める手法。
データベースの専門用語では、テーブルアクセスをスキップしてインデックスだけで処理が完結することを **インデックスオンリースキャン (Index Only Scan)** と呼ぶ。

- **仕組み**: 通常は「1. インデックスで行を探す」→「2. その場所へ行ってデータ（テーブル）を読む」という2段階の手順を踏むが、必要な列がすべてインデックスにあれば「2」をスキップできる。
- **効果**: ボトルネックである「ストレージへのランダムI/O」を劇的に削減できる。「本棚まで本を取りに行かなくても、検索カード（索引）を見るだけで用が済む」状態。

#### 実装方法：付加列インデックス (Included Columns)

`INCLUDE` オプションを使用することで、インデックスキー（検索条件）を増やさずに必要な列をリーフノードに持たせることができる。

```sql
-- 検索条件(FirstName)はキーにし、取得したい列(LastName, Email)はINCLUDEする
CREATE NONCLUSTERED INDEX IX_Person_FirstName
ON Person.Person (FirstName)
INCLUDE (LastName, EmailPromotion);
```

こうすることで、`SELECT LastName, EmailPromotion FROM Person.Person WHERE FirstName = 'Ken'` のようなクエリが、テーブルへのアクセスなしで完結する。

#### トレードオフ（注意点）

高速である反面、以下のリスクやコストが伴う。

1.  **更新オーバーヘッドの増加**: 多くの列をインデックスに含めるため、`INSERT` や `UPDATE` 時の負荷が高くなる。
2.  **仕様変更に弱い**: `SELECT` 句にインデックスに含まれていない列が1つでも追加されると、途端にテーブルアクセス（Key Lookup）が発生し、性能が劣化する。
3.  **制限**: 1つのインデックスに含められる列数には限度がある。

### インデックスが効かない書き方（アンチパターン）

インデックスが存在しても、SQLの書き方によっては利用されない（フルスキャンになる）ことがあるため注意が必要。

1.  **中間一致・後方一致のLIKE検索**: `WHERE col LIKE '%鈴木%'` （前方一致 `鈴木%` ならOK）
2.  **インデックス列に対する演算**: `WHERE col * 1.1 > 100` （`WHERE col > 100 / 1.1` ならOK）
3.  **否定形**: `WHERE col <> 100` （データの大部分を読むことになるため）
4.  **IS NULL検索**: 一般的に効きにくい（状況による）。

### インデックス以外の対処法：データマート

インデックスだけでは解決できない（大量データの集計など）場合、物理設計レベルでの対策として **データマート (Data Mart)** の作成を検討する。
Oracleにおける「マテリアライズド・ビュー (Materialized View)」などもこれに相当する。

- **概要**: 必要なデータだけを抽出・集計した「物理的な別テーブル」を作成しておく。
- **メリット**: 数千万行の集計ではなく、数百行のマートを読み込むだけで済むため、I/Oが劇的に減る。
- **デメリット**: データの鮮度が落ちる（リアルタイムではない）、バッチ処理の運用コストがかかる。

---

## 6. 実践SQLテクニック：結合と集約

論理的なクエリ設計においてパフォーマンスを意識するためのポイント。

### ① サブクエリと結合のリスク

安易なサブクエリや結合を増やすと、計算量が増大し（$N \times M$）、結合アルゴリズムが Nested Loops から Hash Join へと変わり、メモリ不足を引き起こすリスクがある。
基本原則は **「結合する前に行数を減らす」** こと。

### ② 自己結合より「ウィンドウ関数」

同じテーブル同士を結合（Self Join）して比較・集計するのは避ける。

**【悪い例（自己結合）】顧客ごとに一番古い購入履歴を取得**

```sql
SELECT a.cust_id, seq, price
FROM Receipts a
INNER JOIN (
    SELECT cust_id, MIN(seq) as min_seq
    FROM Receipts
    GROUP BY cust_id
) b ON a.cust_id = b.cust_id AND a.seq = b.min_seq;
-- 2回テーブルスキャンが発生し、計算量が増える
```

**【良い例（ウィンドウ関数）】**

```sql
SELECT cust_id, seq, price
FROM (
    SELECT cust_id, seq, price,
           ROW_NUMBER() OVER (PARTITION BY cust_id ORDER BY seq) as row_seq
    FROM Receipts
) a
WHERE a.row_seq = 1;
-- 1回のスキャンで済み、高速かつ計算量が安定する
```

### ③ 結合の前に「集約」（Aggregate before Join）

行数が多いテーブルを結合する場合の鉄則。

**【悪い例】JOINしてからGROUP BY**

```sql
SELECT b.co_cd, SUM(a.emp_nbr)
FROM Companies b
INNER JOIN Shops a ON b.co_cd = a.co_cd
WHERE a.main_flg = 'Y'
GROUP BY b.co_cd;
-- 100万行 × 10行 の結合後に集計を行うため、中間データが膨大になる
```

**【良い例】サブクエリでGROUP BYしてからJOIN**

```sql
SELECT b.co_cd, a.sum_emp
FROM Companies b
INNER JOIN (
    -- ここで先に行数を減らす（100万行 → 数行）
    SELECT co_cd, SUM(emp_nbr) as sum_emp
    FROM Shops
    WHERE main_flg = 'Y'
    GROUP BY co_cd
) a ON b.co_cd = a.co_cd;
-- 集約済みの小さなデータ同士を結合するため、コストが最小限になる
```

### ④ 計算の前に「絞り込み」

集計関数（SUMなど）に全データを渡さず、必要な行だけを渡す。
`WHERE` 句やサブクエリで対象を絞り込んでから計算することで、CPUリソースを節約できる。

---

## 7. インデックスのデメリットとベストプラクティス

インデックスは「魔法の杖」ではなく、コストも伴う機能であることを忘れてはならない。

### デメリット

1.  **更新コストの増加**: `INSERT`, `UPDATE`, `DELETE` の際、データと共にインデックスも更新されるため、書き込み性能が低下する。
2.  **ディスク容量の圧迫**: インデックス自体もストレージ領域を消費する。
3.  **メンテナンスの手間**: 断片化（フラグメンテーション）の解消や、FILLFACTOR（ページ充填率）の調整など、定期的なメンテナンスが必要。

### ベストプラクティス

- **必要最低限に絞る**: 使われていないインデックスは削除する。
- **更新頻度を考慮する**: 頻繁に更新される列へのインデックスは慎重に判断する。
- **カバリングインデックスを活用**: Key Lookup を減らし、I/Oを最小化する。
- **実行プランを確認**: 思い込みで作成せず、実際に `Estimated Subtree Cost` や `SET STATISTICS IO ON` でI/O数を確認して効果を検証する。

---
